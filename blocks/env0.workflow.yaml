environments:
  dynamodb:
    name: dynamodb
    templateName: 'merck-demo-blocks-dynamodb'
  stream_processor_lambda:
    name: stream_processor_lambda
    templateName: 'merck-demo-blocks-lambda'
    needs:
      - dynamodb
settings:
  environmentRemovalStrategy: destroy