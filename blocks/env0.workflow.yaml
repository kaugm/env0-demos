environments:
  VPC:
    name: VPC
    templateName: 'Merck-Demo-VPC'
  SUBNET1:
    name: Subnet1
    templateName: 'Merck-Demo-Subnet'
    needs:
      - VPC

  

settings:
  environmentRemovalStrategy: destroy