environments:
  network:
    name: Network
    templateName: 'workflow-demo-ecs-networking'
  ecs:
    name: ECS Cluster
    templateName: 'workflow-demo-ecs-cluster'
  task1:
    name: Billing Task
    templateName: 'workflow-demo-ecs-task'
    needs:
      - network
      - ecs

settings:
  environmentRemovalStrategy: destroy