version: 2

deploy:
    onSuccess:
      - name: Puppet
        run: |
          terraform output -raw private_key > myKey.pem
          chmod 400 myKey.pem
          IP=$(terraform output -raw public_ip)
          ssh -i myKey.pem ubuntu@$IP 'whoami'
